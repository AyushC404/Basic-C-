#include <stdio.h>

int main(){
    int i=0;
    while(2<10){
        printf("The value of i is %d\n",i);
        i++;// or remove i++ and put while(i<10) without i++
    }
    return 0;
}//Don't run har bad because computer gets heated and kharab 
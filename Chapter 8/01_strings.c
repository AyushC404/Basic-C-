#include <stdio.h>
/*Strings are character array terminated by a null character*/
//Strings are collection of characters
int main(){
    char st[]={'a','b','c','\0'};
    printf("First character is %c\n",st[0]);
    return 0;
}